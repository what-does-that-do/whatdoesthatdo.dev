---
import '@/styles/global.css'

const className = Astro.props.className || "";
---
<div id="gradient" class={`pt-20 p-2 lg:px-10 overflow-hidden bg-gradient-to-tr from-blue-800 to-blue-600 w-full m-0 ${className}`}>
    <slot></slot>
</div>