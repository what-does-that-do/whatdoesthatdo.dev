---
import '../../styles/global.css'

const className = Astro.props.className || "";
---
<div id="gradient" class={`bg-gradient-to-tr from-blue-800 to-blue-600 w-full m-0 ${className}`}>
    <slot></slot>
</div>